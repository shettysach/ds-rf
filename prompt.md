Use the `mjlab-scout` skill for the `portrait-corridors` environment.
Inspect every view yourself, derive the task instructions from what you see,
and write `TASK.md` in the current working directory using the skill's required
format. Do not copy placeholder prose as the answer.
